<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Atualizar Paciente</title>
    <link href="/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <header class="bg-primary text-white text-center py-3">
        <h1>Clínica Médica</h1>
    </header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="/">Início</a>
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/pacientes">Gerenciar Pacientes</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/pacientes/criar">Novo Paciente</a>
                </li>
            </ul>
        </div>
    </nav>
    <main class="container my-5">
        <h2>Atualizar Paciente</h2>
        <% if (error) { %>
            <div class="alert alert-danger"><%= error %></div>
        <% } %>
        <form action="/pacientes/atualizar" method="POST">
            <input type="hidden" name="cpf" value="<%= paciente.cpf %>">
            <div class="mb-3">
                <label for="nome" class="form-label">Nome</label>
                <input type="text" class="form-control" id="nome" name="nome" value="<%= paciente.nome %>" required>
            </div>
            <div class="mb-3">
                <label for="idade" class="form-label">Idade</label>
                <input type="number" class="form-control" id="idade" name="idade" value="<%= paciente.idade %>" required>
            </div>
            <div class="mb-3">
                <label for="diaMarcado" class="form-label">Dia Marcado</label>
                <input type="date" class="form-control" id="diaMarcado" name="diaMarcado" value="<%= paciente.diaMarcado.toISOString().split('T')[0] %>" required>
            </div>
            <div class="mb-3">
                <label for="horaMarcada" class="form-label">Hora Marcada</label>
                <input type="time" class="form-control" id="horaMarcada" name="horaMarcada" value="<%= paciente.horaMarcada %>" required>
            </div>
            <button type="submit" class="btn btn-primary">Atualizar</button>
        </form>
    </main>
    <script src="/js/bootstrap.bundle.min.js"></script>
</body>
</html>
